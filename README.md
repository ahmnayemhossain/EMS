# EMS Platform

Monorepo layout:

- `client/` React + TypeScript + Vite + Tailwind (EMS UI)
- `server/` Express API (local JSON-file store)

## Run (frontend)

```bash
cd client
npm install
npm run dev
```

## Run (backend)

```bash
cd server
npm install
npm run dev
```

