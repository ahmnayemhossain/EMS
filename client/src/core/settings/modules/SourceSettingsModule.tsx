import { MasterWiringModule } from "@/core/settings/modules/master-wiring/module";
import type { SettingsEntity } from "@/core/settings/modules/settingsEntityApi";
import { createSettingsEntity, deleteSettingsEntity, listSettingsEntities, updateSettingsEntity } from "@/core/settings/modules/settingsEntityApi";
import { createSourceWiring, deleteSourceWiring, listSourceWiring, listSourceWiringLookups, updateSourceWiring } from "@/core/settings/modules/uomSettingsApi";

export function SourceSettingsModule() {
  return <MasterWiringModule config={{ kind: "sources", singularLabel: "Source", pluralLabel: "Sources", relationLabel: "Source", relationPlaceholder: "Select source", createSuccess: "Source created", updateSuccess: "Source updated", deleteSuccess: "Source deleted", createWiringSuccess: "Source wiring created", updateWiringSuccess: "Source wiring updated", deleteWiringSuccess: "Source wiring deleted" }} api={{ listEntities: (userId) => listSettingsEntities("sources", userId), createEntity: (item, userId) => createSettingsEntity("sources", item, userId), updateEntity: (item, userId) => updateSettingsEntity("sources", item, userId), deleteEntity: (id, userId) => deleteSettingsEntity("sources", id, userId), listWiring: async (userId) => (await listSourceWiring(userId)).map((row) => ({ ...row, relationId: row.sourceId, relationName: row.sourceName })), listLookups: async (userId) => { const data = await listSourceWiringLookups(userId); return { relationOptions: data.sourceOptions as SettingsEntity[], utilityTypeOptions: data.utilityTypeOptions }; }, createWiring: async (item, userId) => { const row = await createSourceWiring({ sourceId: item.relationId, utilityTypeId: item.utilityTypeId, status: item.status }, userId); return { ...row, relationId: row.sourceId, relationName: row.sourceName }; }, updateWiring: async (item, userId) => { const row = await updateSourceWiring({ id: item.id, sourceId: item.relationId, utilityTypeId: item.utilityTypeId, status: item.status }, userId); return { ...row, relationId: row.sourceId, relationName: row.sourceName }; }, deleteWiring: deleteSourceWiring }} />;
}
