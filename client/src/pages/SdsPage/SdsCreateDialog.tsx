import { Input } from "@/app/components/ui/input";
import { Separator } from "@/app/components/ui/separator";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/app/components/ui/tabs";
import { Textarea } from "@/app/components/ui/textarea";
import { CreateActionDialog } from "@/components/CreateActionDialog";
import { SelectFilter } from "@/components/SelectFilter";

import { SDS_SECTION_TABS } from "./constants";

type SdsCreateDialogProps = { open: boolean; onOpenChange: (open: boolean) => void; createMeta: { chemicalName: string; supplier: string; language: string; revisionDate: string; notes: string }; setCreateMeta: (updater: (previous: SdsCreateDialogProps["createMeta"]) => SdsCreateDialogProps["createMeta"]) => void; createTab: string; setCreateTab: (value: string) => void; createDraftBySectionId: Record<string, string>; setCreateDraftBySectionId: (updater: (previous: Record<string, string>) => Record<string, string>) => void; createErrors: Record<string, string>; sectionTitleById: Record<string, string>; onCreate: () => Promise<boolean>; };

export function SdsCreateDialog(props: SdsCreateDialogProps) {
  return <CreateActionDialog title="Create SDS record" open={props.open} onOpenChange={props.onOpenChange} contentClassName="sm:max-w-2xl max-h-[80vh] overflow-y-auto" onCreate={props.onCreate}><div className="grid gap-3 sm:grid-cols-2"><div className="grid gap-1.5 sm:col-span-2"><div className="text-muted-foreground text-xs">Chemical name</div><Input value={props.createMeta.chemicalName} onChange={(event) => props.setCreateMeta((prev) => ({ ...prev, chemicalName: event.target.value }))} className={props.createErrors.chemicalName ? "border-destructive" : ""} placeholder="Chemical name" /></div><div className="grid gap-1.5"><div className="text-muted-foreground text-xs">Supplier</div><Input value={props.createMeta.supplier} onChange={(event) => props.setCreateMeta((prev) => ({ ...prev, supplier: event.target.value }))} className={props.createErrors.supplier ? "border-destructive" : ""} placeholder="Supplier" /></div><div className="grid gap-1.5"><div className="text-muted-foreground text-xs">Language</div><SelectFilter value={props.createMeta.language || undefined} onChange={(value) => props.setCreateMeta((prev) => ({ ...prev, language: value }))} placeholder="Select language" items={[{ value: "English", label: "English" }, { value: "Bangla", label: "Bangla" }]} /></div><div className="grid gap-1.5"><div className="text-muted-foreground text-xs">Revision date</div><Input type="date" value={props.createMeta.revisionDate} onChange={(event) => props.setCreateMeta((prev) => ({ ...prev, revisionDate: event.target.value }))} className={props.createErrors.revisionDate ? "border-destructive" : ""} /></div><div className="grid gap-1.5 sm:col-span-2"><div className="text-muted-foreground text-xs">File</div><div className="text-muted-foreground rounded-xl border border-dashed p-4 text-sm">Upload SDS PDF (placeholder)</div></div><div className="grid gap-1.5 sm:col-span-2"><div className="text-muted-foreground text-xs">Notes</div><Textarea value={props.createMeta.notes} onChange={(event) => props.setCreateMeta((prev) => ({ ...prev, notes: event.target.value }))} placeholder="Optional notes" /></div></div><Separator className="my-1" /><Tabs value={props.createTab} onValueChange={props.setCreateTab} className="w-full"><TabsList className="bg-muted/30 grid h-auto w-full grid-cols-2 gap-1 rounded-xl border p-1 sm:grid-cols-4">{SDS_SECTION_TABS.map((tab) => <TabsTrigger key={tab.id} value={tab.id} className="gap-2"><span className="text-xs font-semibold">{tab.label}</span></TabsTrigger>)}</TabsList>{SDS_SECTION_TABS.map((tab) => <TabsContent key={tab.id} value={tab.id} className="mt-3 m-0"><div className="grid gap-3">{tab.sectionIds.map((sectionId) => <div key={sectionId} className="grid gap-1.5"><div className="text-muted-foreground text-xs">Section {sectionId}: {props.sectionTitleById[sectionId] ?? `Section ${sectionId}`}</div><Textarea value={props.createDraftBySectionId[sectionId] ?? ""} onChange={(event) => props.setCreateDraftBySectionId((prev) => ({ ...prev, [sectionId]: event.target.value }))} rows={4} className={props.createErrors[`sec_${sectionId}`] ? "border-destructive" : ""} placeholder="Type section details..." /></div>)}</div></TabsContent>)}</Tabs></CreateActionDialog>;
}
